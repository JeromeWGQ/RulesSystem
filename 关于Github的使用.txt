    我们每个人负责一个功能板块，独立完成板块的前端、后台，当遇到交叉功能时（比如显示某一文档的界面、数据库的建立），一起商议。
    不要把代码直接提交到master分支上，以防止冲突。
    提交的文件仅包括：源代码、web文件夹、sql脚本文件。一切生成文件、工程配置文件（.iml, .classpath）等不要上传。
    如果自己用到的css文件名称有冲突，重命名自己的css文件，不要覆盖原来的。